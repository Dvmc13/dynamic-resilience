rules:
  - condition: "latency > 10"
    action: "circuitBreaker.open()"
  - condition: "latency < 9"
    action: "retry.maxAttempts = 3"
  - condition: "errorRate > 0.2"
    action: "circuitBreaker.open()"
