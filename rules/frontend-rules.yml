# En tu repo: dynamic-resilience/rules/frontend-rules.yml
resilience4j:
  circuitbreaker:
    instances:
      frontendCircuitBreaker:
        failureRateThreshold: 50    # % de errores para abrir
        waitDurationInOpenState: 30s
        metrics:
          enabled: true
