# rules/frontend-circuitbreaker.yml
circuit_breakers:
  - name: frontendCircuitBreaker
    base_config: default
    overrides:
      failure_rate_threshold: 50       # % de errores para abrir
      wait_duration_in_open_state: 30s # Tiempo en estado abierto
      sliding_window_size: 10          # Ventana de métricas (llamadas)
