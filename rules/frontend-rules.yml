rules:
  # Regla 1: Si la latencia supera 250 ms, abrir el Circuit Breaker
  - condition: "latency > 250"
    action: "circuitBreaker.open()"
  # Regla 2: Si la latencia es menor o igual a 200 ms, cerrar el Circuit Breaker
  - condition: "latency <= 200"
    action: "circuitBreaker.close()"
